name: Plan Docs Gate

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    steps:
      - run: echo "docs-only change"

  postgres-tests:
    name: postgres-tests
    runs-on: ubuntu-latest
    steps:
      - run: echo "docs-only change"

  browser-smoke:
    name: browser-smoke
    runs-on: ubuntu-latest
    steps:
      - run: echo "docs-only change"

  security:
    name: security
    runs-on: ubuntu-latest
    steps:
      - run: echo "docs-only change"
