name: M13 Format Diagnostic

on:
  push:
    branches:
      - feat/m13-reports-core

permissions:
  contents: read

jobs:
  pint-diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          tools: composer:v2
      - run: composer install --no-interaction --prefer-dist --no-progress
      - name: Show Pint diff
        run: |
          vendor/bin/pint app/Modules/Reports/ReportService.php app/Modules/Reports/ReportsController.php
          git diff --no-color -- app/Modules/Reports/ReportService.php app/Modules/Reports/ReportsController.php
