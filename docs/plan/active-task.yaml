id: PLAN-010
title: Logical database model
module: db
status: ready
progress: not_started

predecessor:
  id: PLAN-009
  status: completed_frozen
  plan: docs/plan/11-iadeler-rma/

phase:
  id: P3
  name: Logical Database Model
  status: ready

goal:
  Convert the completed/frozen P2 workflow contracts into a normalized logical PostgreSQL domain model without writing physical SQL migrations or application code.

scope:
  allowed_paths:
    - docs/db/
    - docs/plan/project-state.yaml
    - docs/plan/active-task.yaml
    - docs/plan/handoff/current.md
    - docs/plan/tasks/
  forbidden_paths:
    - src/
    - tests/
  physical_sql_implementation: false
  migration_implementation: false
  api_implementation: false
  ui_implementation: false

known_dependencies:
  - P2 Core Commercial Workflow Planning is COMPLETED / FROZEN.
  - PLAN-009 Returns / RMA is COMPLETED / FROZEN.
  - authoritative ledgers, snapshots, source-target lineage and reversal rules from PLAN-002 through PLAN-009 must be preserved.
  - logical model must trace every authoritative entity to accepted workflow requirements.

planning_progress:
  counting_basis: master-project-plan section 8; completed/frozen packages only
  master_sequence:
    completed: 9
    total: 30
    percent: 30.0
  p2_core_commercial:
    completed: 8
    total: 8
    percent: 100.0

next_action:
  id: PLAN-010
  title: Logical database model

test_policy:
  heavy_tests: FULL_TEST_DAY_ONLY
  normal_session: documentation/model consistency checks only
