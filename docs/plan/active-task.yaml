id: P4
title: Foundation implementation readiness / decision-gate resolution
module: foundation
status: ready_for_governance_decisions
progress: not_started

phase:
  id: P4
  name: Foundation implementation
  status: readiness_decision_gates

predecessor:
  id: PLAN-010
  title: Logical database model
  status: completed_frozen
  phase: P3

goal:
  Resolve only the Foundation technology decisions required to safely begin the first P4 implementation slice. Do not implement code in this readiness task.

authoritative_order_resolution:
  next_execution_phase: P4
  reasoning:
    - master-project-plan phase map explicitly orders P3 -> P4 -> P5 -> P6 -> P7
    - master-project-plan section 8 is the full module planning sequence and does not override phase execution order
    - Quality is section-8 item 10 but operational execution belongs P6
    - Commerce belongs P7; prior immediate PLAN-011 Commerce backlog entry was stale
  task_numbering:
    - do not invent a new PLAN number for this readiness work
    - use existing phase ID P4 until repository assigns an implementation work-package ID

required_readiness_work:
  - review Foundation decision gates in docs/plan/01-foundation/framework-plan.md and master-project-plan.md
  - classify each gate as required for first P4 thin slice vs safely deferrable
  - obtain and record explicit owner decisions for required-now gates
  - update Foundation contract and ADR/state only after decisions are explicit
  - only then prepare a separate implementation task

known_decision_gates:
  - exact .NET SDK/runtime version
  - exact ORM/data-access library
  - exact auth/identity provider
  - exact OpenAPI tooling
  - exact logging/metrics stack
  - exact object/file storage backend
  - exact Desktop shell technology
  - exact Mobile shell technology
  - optional scheduling library
  - production secret store
  - production reverse proxy/tunnel details

scope:
  allowed_paths:
    - docs/plan/01-foundation/
    - docs/plan/decisions/
    - docs/plan/project-state.yaml
    - docs/plan/active-task.yaml
    - docs/plan/handoff/current.md
    - docs/plan/tasks/
    - docs/plan/roadmap.md
  forbidden_paths:
    - src/
    - tests/
  physical_sql_implementation: false
  migration_implementation: false
  api_implementation: false
  ui_implementation: false
  deployment: false

planning_progress:
  counting_basis: master-project-plan section 8; completed/frozen section-8 packages only
  master_sequence:
    completed: 9
    total: 30
    percent: 30.0
    note: Quality is section-8 item 10; P4 readiness/implementation does not increment this planning metric.
  p2_core_commercial:
    completed: 8
    total: 8
    percent: 100.0

test_policy:
  heavy_tests: FULL_TEST_DAY_ONLY
  readiness_session: documentation/decision consistency checks only
