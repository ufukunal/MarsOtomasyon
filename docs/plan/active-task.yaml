id: FW-IMP-002
title: configuration/context/error primitives
module: foundation
status: ready
progress: not_started

phase:
  id: P4
  name: Foundation implementation
  status: implementation_in_progress

predecessor:
  id: FW-IMP-001
  title: repository solution skeleton
  status: completed
  evidence:
    commit: 212022601c177ec6bc8e8438c6188e747cbee2c5
    workflow_run: 35702737435
    build: success
    sdk: 10.0.401
    runtime: 10.0.12

goal:
  Implement only the Foundation configuration, request/job context and result/error primitives defined by the accepted framework contract.

scope:
  include:
    - typed configuration primitives and startup validation contracts
    - correlation context primitive
    - actor/company context interfaces
    - result/error contracts
    - targeted unit tests for implemented primitives
  exclude:
    - domain business rules
    - PostgreSQL/EF Core mappings or migrations
    - auth/identity provider selection or implementation
    - OpenAPI tooling
    - production logging/metrics provider
    - file storage
    - Mars.Web/Mars.UI
    - deployment

accepted_baselines:
  runtime: .NET 10 LTS
  persistence: EF Core 10 + Npgsql for later persistence package

planning_progress:
  master_sequence:
    completed: 9
    total: 30
    percent: 30.0
  p2_core_commercial:
    completed: 8
    total: 8
    percent: 100.0

test_policy:
  normal:
    - build
    - targeted unit tests for FW-IMP-002 only
  heavy_tests: FULL_TEST_DAY_ONLY
