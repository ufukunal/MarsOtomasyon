id: PLAN-003
title: Detail Party / Customer / Supplier model
module: 03-cariler
status: ready
progress: not_started

predecessor:
  id: PLAN-002
  status: completed_frozen
  evidence_commit: a5f56208b44847cf74feba43934f6d25412e93ad
  sales_plan: docs/plan/05-satis/

goal:
  Freeze the Party / Customer / Supplier domain model and workflows before logical database design or application implementation.

scope:
  allowed_paths:
    - docs/plan/03-cariler/
    - docs/plan/project-state.yaml
    - docs/plan/active-task.yaml
    - docs/plan/handoff/current.md
    - docs/plan/tasks/
  forbidden_paths:
    - src/
    - tests/
    - docs/db/schema/
  db_implementation: false
  api_implementation: false
  ui_implementation: false
  integration_implementation: false

required_start:
  - verify current main HEAD
  - read docs/plan/ai-cmd.md
  - read docs/ai/skill-router.md
  - read docs/plan/master-project-plan.md
  - read frozen Sales contracts under docs/plan/05-satis/
  - inspect existing docs/plan/03-cariler/ before planning
  - route exact primary/reviewer skills before changing files

known_dependency_from_sales:
  - Party/customer identity must support immutable historical snapshots in Quote, Order and Invoice.
  - Customer account balance remains Finance-ledger-derived; no mutable customer balance authority.
  - Sales Collection is balance-only, not Invoice allocation.
  - Sales approval may consume authoritative customer/policy risk signals when later defined, but PLAN-003 must not invent Finance policy.

next_action:
  id: PLAN-003
  title: Detail Party / Customer / Supplier model

test_policy:
  heavy_tests: FULL_TEST_DAY_ONLY
  normal_session: documentation consistency checks only
