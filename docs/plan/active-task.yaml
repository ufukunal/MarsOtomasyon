id: FW-IMP-007
title: Docker/test deployment baseline
module: foundation
status: ready
progress: not_started

phase:
  id: P4
  name: Foundation implementation
  status: implementation_in_progress

predecessor:
  id: FW-IMP-006
  title: Mars.Web + Mars.UI foundation
  status: completed
  evidence:
    implementation_commit: 1264981655329099a086c7048c0890caf04dc9f2
    workflow_run: 35729371845
    frontend_tests: 10_passed
    frontend_typecheck: success
    frontend_build: success
    static_architecture_checks: success
    dotnet_build: success
    dotnet_targeted_tests: 26_passed
    canonical_report: docs/plan/01-foundation/fw-imp-006-implementation.md

goal:
  Implement the repository-defined Docker/test deployment baseline against the separate MarsOtomasyon test server without turning test-environment choices into production architecture.

source_basis:
  - docs/plan/01-foundation/framework-plan.md sections 39-42
  - docs/plan/01-foundation/test-environment.md
  - docs/plan/01-foundation/p4-readiness-decision-gates.md

scope:
  include:
    - application images/containers required by current Foundation
    - Docker Compose test baseline
    - migration/startup policy
    - test-server deployment
    - readiness checks
    - small smoke evidence
  exclude:
    - production deployment
    - production secret-store selection
    - production reverse-proxy/tunnel selection
    - structured logging/metrics technology selection unless a narrow test-deployment blocker is proven
    - Desktop/Mobile shell technology
    - ERP domain feature implementation

preflight_required:
  - verify current test Docker version
  - verify current test Docker Compose version
  - verify deployment/service layout
  - verify test URL/DNS/TLS facts relevant to the selected test path
  - read test credentials only from the canonical source and never echo values

decision_gate_review:
  blocking_owner_decision: none_identified
  deferred:
    - production secret store
    - production reverse proxy/tunnel
    - structured logging/metrics stack
  note:
    - Unknown test-environment facts must be verified at runtime rather than guessed.
    - Test architecture must not be described as accepted production architecture.

planning_progress:
  master_sequence:
    completed: 9
    total: 30
    percent: 30.0
  p2_core_commercial:
    completed: 8
    total: 8
    percent: 100.0

test_policy:
  normal_development:
    - image/build checks
    - compose/config validation
    - migration/startup safety checks
    - deploy to test
    - readiness
    - small smoke
  heavy_tests: FULL_TEST_DAY_ONLY
