# M5 — Teklifler / Tax Calculation Contract

**Wave:** B  
**Durum:** 🟡 DEVAM EDİYOR — M5.1–M5.4 kapalı, M5.5 aktif  
**Entry:** M2 Cari + M3 Ürün. ✅

## - [x] M5.1 — Deterministic Tax Calculator
- [x] Net/gross/tax; KDV dahil/hariç normalization
- [x] Line/document discount, rounding, tax-zero reason
### Tamamlanma Raporu
- **Durum:** TAMAMLANDI
- **Tarih:** 2026-08-25
- **PR / Merge:** `#28` → `a7c4d8be5bda71fb1d6d7e873f12d1eb3a878011`
- **Kanıt:** Exact 6-decimal `Decimal6` hesap çekirdeği; PHP float aritmetiği yok; `ROUND_HALF_UP`; net/gross (KDV hariç/dahil) normalization; gross input toplamını koruyan geriye çözümleme; satır ve belge indiriminin sıralı uygulanması; vergi sonrası deterministik line/document totals; `%0` vergi için canonical zero-reason zorunluluğu; taxable satırda zero-reason yasağı; duplicate line identity, invalid quantity/rate ve numeric overflow guard'ları.
- **CI/Test:** PR Foundation `32896949216` 4/4 green; main Foundation `32897322473` 4/4 green; PostgreSQL `268 test / 2198 assertion`; `DeterministicTaxCalculatorTest` 7/7 fixture green; Chromium browser smoke green.
- **Blocker:** Yok.

## - [x] M5.2 — Quote Schema + CRUD
- [x] Header/lines, list/create/detail, draft edit/status
### Tamamlanma Raporu
- **Durum:** TAMAMLANDI
- **Tarih:** 2026-08-26
- **PR / Merge:** `#29` → `e19b2a7b6f561fd5c33f98d38b1b33c17b2f027a`
- **Kanıt:** Company-scoped `quotes` / `quote_lines`; tenant-safe composite FK'ler; exact `NUMERIC(20,6)` ticari alanlar ve header/line reconciliation CHECK'leri; mevcut transaction-safe `DocumentNumberIssuer` ile teklif numarası; `quotes.view` / `quotes.manage` RBAC; draft create/update/cancel akışı; cross-company route isolation; audit kayıtları; list/create/detail/edit Blade ekranları. Request'ten gönderilen sahte toplamlar persist edilmez; tüm `base/net/tax/gross` alanları M5.1 `DeterministicTaxCalculator` authority'sinden üretilir. Draft edit belge kimliğini değiştirmez, iptal sonrası edit reddedilir.
- **CI/Test:** PR Foundation `32900526333` 4/4 green; main Foundation `32901176669` 4/4 green; PostgreSQL `271 test / 2271 assertion`; `QuoteCrudTest` 3/3 green; Pint + Larastan + security + Chromium browser smoke green.
- **Blocker:** Yok.

## - [x] M5.3 — Revision Management
- [x] Revision snapshot/history/immutability
### Tamamlanma Raporu
- **Durum:** TAMAMLANDI
- **Tarih:** 2026-08-26
- **PR / Merge:** `#30` → `17bf941ec83333c05843e2cbd0f02e393f0866d4`
- **Kanıt:** Company-scoped `quote_revisions` / `quote_revision_lines`; quote-row lock altında monotonic revision numbering; persisted server-authoritative teklif içeriğinden immutable ticari snapshot; SHA-256 content fingerprint ile aynı snapshot'ın replay-safe dedup edilmesi; account/product/tax kimlik ve açıklama alanlarının self-contained snapshot olarak dondurulması; PostgreSQL UPDATE/DELETE immutability trigger'ları; revision audit event/target; teklif detayında revision history ve readonly snapshot detayı; mevcut `quotes.view/manage` RBAC ve tenant izolasyonu. Draft değiştikten sonra yeni revision oluşurken önceki revision değişmez. Approval/reject/Quote→Order bu dilimde bilinçli olarak yapılmadı ve M5.4 sınırında tutuldu.
- **CI/Test:** PR Foundation `32903243048` 4/4 green; main Foundation `32903576353` 4/4 green; PostgreSQL `274 test / 2324 assertion`; `QuoteRevisionTest` 3/3 green; Pint + Larastan + security + Chromium browser smoke green.
- **Blocker:** Yok.

## - [x] M5.4 — Approval + Quote→Order
- [x] Approval guards, selected revision, idempotent conversion, lineage
### Tamamlanma Raporu
- **Durum:** TAMAMLANDI
- **Tarih:** 2026-08-26
- **PR / Merge:** `#31` → `5bce3df4bf6db0132ac6e562628b256c77f6cd82`
- **Kanıt:** Draft yönetiminden bağımsız `quotes.approve` ticari yetkisi; explicit immutable revision seçimi; `draft → approved/rejected`, `approved → converted` lifecycle; finalized quote header/satır PostgreSQL freeze; seçilen revision'dan transaction-safe ve exactly-once Quote→SalesOrder conversion; source quote/revision/revision-line lineage; mevcut `DocumentNumberIssuer` üzerinden `sales_order` numbering; conversion-result minimal `sales_orders` / `sales_order_lines` çekirdeği; approval/rejection/order/conversion audit eventleri; finalized teklif ekranında mutable quote yerine seçili revision'ın ticari otorite olarak gösterilmesi. Eski R1 açıkça onaylanmışsa current draft daha sonra R2 içeriği taşısa bile sipariş R1'den üretilir. Full Sales Order CRUD/reservation/progress M6 sınırında kalır.
- **Acceptance:** `QuoteApprovalConversionTest` 4/4 green; ayrı RBAC, rejection terminality/replay, older-R1 exactly-once conversion, numbering idempotency, tenant/lineage ve raw-SQL immutability guard'ları; Chromium gerçek akış `login → snapshot → approve → convert → SO-0001` green.
- **CI/Test:** Final PR Foundation `32905661066` (#359) exact head `14641152b4a500e183f58b30eacc6a3e5d512d6a` 4/4 green; main Foundation `32906064382` (#360) merge SHA üzerinde 4/4 green; PostgreSQL `278 test / 2401 assertion`; Pint + Larastan + security + Chromium browser smoke green.
- **Blocker:** Yok.

## - [ ] M5.5 — PDF / Finalized Detail
- [ ] Fixed/versioned PDF + readonly finalized snapshot
### İlerleme Raporu
- **Durum:** AKTİF
- **Entry:** M5.4 merge `5bce3df4bf6db0132ac6e562628b256c77f6cd82`; PR/main Foundation 4/4 green; `278 test / 2401 assertion`.
- **Bu dilimde tamamlanacak:** Finalized detail yalnız explicit `selected_revision_id` immutable snapshotından okunur; draft belge finalized/PDF yüzeyine giremez; versioned PDF renderer; ilk üretimde exact byte/hash'i private storage + `FileAsset` metadata ile dondurulan ve tekrar indirmede yeniden render edilmeyen PDF; tenant-safe download; readonly finalized UI; PDF/source lineage ve PostgreSQL immutability; PDF magic/MIME/hash/idempotency acceptance + Chromium finalized-detail akışı.
- **Commit/CI:** —

## - [ ] M5.6 — Teklif Exit Gate
- [ ] Calculator/revision/conversion/PDF regression + CI
### Tamamlanma Raporu
- **Durum:** BEKLİYOR | **CI:** — | **Blocker:** —

## Değişmez Kurallar
- Aynı input deterministik aynı hesap sonucu verir.
- Teklif toplamları request'ten güvenilmez; server-side M5.1 calculator authority'sinden üretilir.
- Revision snapshot yalnız server-authoritative persisted teklif durumundan üretilir ve oluşturulduktan sonra append-only / immutable kalır.
- Approval explicit immutable revision seçer; approval sonrasında seçilen ticari snapshot mutate edilmez.
- Quote→Order conversion yalnız approved seçili revision'dan exactly-once üretilir; lineage kaybolmaz.
- Finalized detail ve PDF yalnız selected immutable revision'dan üretilir; mutable quote draft alanları PDF authority'si değildir.
- Üretilmiş finalized PDF byte/hash olarak sabittir; download sırasında yeniden render edilmez.
- Finalized revision mutate edilmez; zero-reason server-side zorunlu.

## Milestone Kapanış Raporu
- **Durum:** DEVAM EDİYOR — M5.1–M5.4 kapalı; M5.5 aktif.
- **Tarih/Commit/CI:** 2026-08-26 / M5.4 `5bce3df4…`; PR Foundation `32905661066`; main Foundation `32906064382`; PostgreSQL `278 / 2401`.
- **Aktif kesin iş:** M5.5 PDF / Finalized Detail.
- **Blocker:** Yok.
